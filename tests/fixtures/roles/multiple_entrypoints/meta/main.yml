---
galaxy_info:
  role_name: multiple_entrypoints
  author: your name
  description: Test role with a multiple_entrypoints amount of metadata
  company: ansible-docs

  license: MIT

  min_ansible_version: "1.2"

  platforms:
    - name: Fedora
      versions:
        - all

argument_specs:
  main:
    short_description: The main entry point for the multiple_entrypoints role.
    options:
      myapp_int:
        type: "int"
        required: false
        default: 42
        description: "The integer value, defaulting to 42."
      myapp_str:
        type: "str"
        required: true
        description: "The string value"
  second:
    short_description: The second entry point for the multiple_entrypoints role.
    options:
      myapp_int_second:
        type: "int"
        required: false
        default: 42
        description: "The integer value, defaulting to 42."
      myapp_str_second:
        type: "str"
        required: true
        description: "The string value"
